# ---- Supabase (login, clients, contracts, tasks, leads, processes, cora, gclick, payroll) ----
VITE_SUPABASE_PROJECT_ID="rvekakbpmkemgiwkkdok"
VITE_SUPABASE_URL="https://rvekakbpmkemgiwkkdok.supabase.co"
VITE_SUPABASE_PUBLISHABLE_KEY=""

# ---- Postgres financeiro self-hosted (via PostgREST no EasyPanel) ----
# Apontar para o dominio publico do PostgREST. Ex: https://finance.seudominio.com.br
# Deixar VAZIO para o app continuar usando o Supabase para o modulo financeiro
# (modo fallback transparente durante a transicao).
VITE_LOCAL_DB_URL=""

# JWT fixo com payload {"role":"anon"}, assinado com o JWT_SECRET do Supabase.
# Gerar com:
#   node -e "const j=require('jsonwebtoken');console.log(j.sign({role:'anon'},'<SECRET>',{expiresIn:'10y'}))"
VITE_LOCAL_DB_ANON_KEY=""
